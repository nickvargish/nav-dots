#!/bin/sh

for f in emacs gnus zshenv zshrc zshalii tmux.*; do
  if [ -e $HOME/.$f ] ; then
    mv $HOME/.$f $HOME/.$f.bak
  fi
  cp -R $f $HOME/.$f
done

if [ ! -d $HOME/bin ]; then
  mkdir $HOME/bin
fi
cp -R zsh.fn $HOME/bin
cp -R noarch $HOME/bin
