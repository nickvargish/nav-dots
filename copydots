#!/bin/sh

for f in emacs gnus zshenv zshrc zsh zshalii tmux.conf; do
  if [ -e $HOME/.$f ] ; then
    rm -rf $HOME/.$f.bak
    mv $HOME/.$f $HOME/.$f.bak
  fi
  cp -R $f $HOME/.$f
done

if [ ! -d $HOME/bin ]; then
  mkdir $HOME/bin
fi
cp -R noarch $HOME/bin
